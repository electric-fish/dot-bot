module.exports = (message) => {
//   let member = member.mentions.members.first();
//   let member = message.mentions.first();
//   console.log("test");
//   return message.reply(member.author.username + " mentioned <@" + member.id + ">");
  return message.reply("test");
};